import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shoogland.timmerdorp',
  appName: 'timmerdorp-web-app',
  webDir: 'dist'
};

export default config;
